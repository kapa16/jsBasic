в случае применения
closeImageElement.addEventListener('click', this.close);
теряется контекст, насколько я понял в this передается передается внешний контекст - обертка галереи, а не
кнопка закрытия.
Для того, чтобы передался элемент, вызвавший событие click, нужно сделать замыкание с помощью
стрелочной функции, тогда this сохранит вызвавший событие контекст и при срабатывании события в this будет
элемент, вызвавший событие